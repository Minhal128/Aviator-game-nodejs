{
  "name": "@ludo/client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Ludo Royale — Phaser 4 game client: board, match HUD, the six juice effects and real-time online play (Colyseus). Offline vs CPU and local pass & play included.",
  "scripts": {
    "dev": "node scripts/gen-icons.mjs && vite",
    "typecheck": "tsc -p tsconfig.json",
    "gen:icons": "node scripts/gen-icons.mjs",
    "build": "node scripts/gen-icons.mjs && tsc -p tsconfig.json && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@colyseus/sdk": "^0.17.0",
    "@ludo/shared": "*",
    "phaser": "^4.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  }
}
